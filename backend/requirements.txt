fastapi==0.115.5
uvicorn[standard]==0.32.1
yfinance>=0.2.65
curl_cffi>=0.7.4
pandas==2.2.3
numpy==2.1.3
# HTML parser backends for pandas.read_html (Wikipedia universe scraping in
# universe.py). NOT pulled transitively — yfinance only brings beautifulsoup4,
# which alone is insufficient for read_html. Clean installs (e.g. Render) need
# these explicitly, otherwise the scan fails with "universe unavailable".
lxml>=5.3.0
html5lib>=1.1
python-dotenv==1.0.1
anthropic>=0.40.0
